meta {
  name: Delete a Shipping Profile
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/shipping-profiles/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
