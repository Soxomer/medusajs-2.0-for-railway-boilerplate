meta {
  name: Update a Customer-s Address
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/admin/customers/:id/addresses/:address_id
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  address_id: 
}

auth:bearer {
  token: {{token}}
}
