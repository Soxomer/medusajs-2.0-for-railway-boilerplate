meta {
  name: Add a Customer Address
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/admin/customers/:id/addresses
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
