meta {
  name: List Addresses
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/admin/customers/:id/addresses/:address_id
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  address_id: 
}

auth:bearer {
  token: {{token}}
}
