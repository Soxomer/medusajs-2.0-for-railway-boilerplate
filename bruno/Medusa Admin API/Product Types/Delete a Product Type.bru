meta {
  name: Delete a Product Type
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/product-types/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
