meta {
  name: Create an Order Fulfillment
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/admin/orders/:id/fulfillments
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
