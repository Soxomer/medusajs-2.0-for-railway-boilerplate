meta {
  name: Cancel Order
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/admin/orders/:id/cancel
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
