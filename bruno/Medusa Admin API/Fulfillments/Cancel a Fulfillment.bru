meta {
  name: Cancel a Fulfillment
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/fulfillments/:id/cancel
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
