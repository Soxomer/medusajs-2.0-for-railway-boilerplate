meta {
  name: Create Draft Order
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/draft-orders
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

auth:bearer {
  token: {{token}}
}
