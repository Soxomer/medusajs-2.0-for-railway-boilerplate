meta {
  name: Subscribe to Step of a Workflow-s Execution
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/admin/workflows-executions/:workflow_id/:transaction_id/:step_id/subscribe
  body: none
  auth: bearer
}

params:path {
  workflow_id: 
  transaction_id: 
  step_id: 
}

auth:bearer {
  token: {{token}}
}
