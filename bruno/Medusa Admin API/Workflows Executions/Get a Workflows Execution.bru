meta {
  name: Get a Workflows Execution
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/workflows-executions/:id
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
