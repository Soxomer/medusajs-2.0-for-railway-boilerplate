meta {
  name: Delete a Price Preference
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/price-preferences/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
