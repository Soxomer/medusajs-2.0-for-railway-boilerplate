meta {
  name: Delete a Reservation
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/reservations/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
