meta {
  name: Delete a Tax Region
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/tax-regions/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
