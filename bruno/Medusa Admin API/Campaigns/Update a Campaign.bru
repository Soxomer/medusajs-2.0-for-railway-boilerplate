meta {
  name: Update a Campaign
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/campaigns/:id
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
