meta {
  name: List Rules of a Promotion
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/admin/promotions/:id/:rule_type
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  rule_type: 
}

auth:bearer {
  token: {{token}}
}
