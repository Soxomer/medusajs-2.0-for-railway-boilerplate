meta {
  name: Delete a Payment Collection
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/admin/payment-collections/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
