meta {
  name: Create Payment Collection
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/payment-collections
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "order_id": "",
    "amount": ""
  }
}
