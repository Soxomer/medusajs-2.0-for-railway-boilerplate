meta {
  name: Manage Sales Channels of a Stock Location
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/admin/stock-locations/:id/sales-channels
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "add": [],
    "remove": []
  }
}
