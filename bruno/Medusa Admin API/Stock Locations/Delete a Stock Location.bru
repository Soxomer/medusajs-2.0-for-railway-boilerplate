meta {
  name: Delete a Stock Location
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/stock-locations/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
