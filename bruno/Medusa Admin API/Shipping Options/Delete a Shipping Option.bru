meta {
  name: Delete a Shipping Option
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/shipping-options/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
