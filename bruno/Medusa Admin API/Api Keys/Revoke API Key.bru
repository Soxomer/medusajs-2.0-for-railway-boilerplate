meta {
  name: Revoke API Key
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/admin/api-keys/:id/revoke
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "revoke_in": ""
  }
}
