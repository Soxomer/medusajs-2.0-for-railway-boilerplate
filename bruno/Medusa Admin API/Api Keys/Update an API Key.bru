meta {
  name: Update an API Key
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/api-keys/:id
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": ""
  }
}
