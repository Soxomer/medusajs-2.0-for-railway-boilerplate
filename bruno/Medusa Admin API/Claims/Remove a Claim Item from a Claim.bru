meta {
  name: Remove a Claim Item from a Claim
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/admin/claims/:id/claim-items/:action_id
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  action_id: 
}

auth:bearer {
  token: {{token}}
}
