meta {
  name: Remove an Inbound Item from Claim
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/admin/claims/:id/inbound/items/:action_id
  body: none
  auth: bearer
}

params:path {
  id: 
  action_id: 
}

auth:bearer {
  token: {{token}}
}
