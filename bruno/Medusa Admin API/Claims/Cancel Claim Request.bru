meta {
  name: Cancel Claim Request
  type: http
  seq: 21
}

delete {
  url: {{baseUrl}}/admin/claims/:id/request
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
