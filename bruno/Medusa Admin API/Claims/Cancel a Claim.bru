meta {
  name: Cancel a Claim
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/claims/:id/cancel
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "no_notification": ""
  }
}
