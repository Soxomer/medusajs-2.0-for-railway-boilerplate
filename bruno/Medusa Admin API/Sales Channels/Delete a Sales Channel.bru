meta {
  name: Delete a Sales Channel
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/sales-channels/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
