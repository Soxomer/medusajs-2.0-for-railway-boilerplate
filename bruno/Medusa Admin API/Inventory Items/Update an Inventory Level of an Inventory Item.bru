meta {
  name: Update an Inventory Level of an Inventory Item
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/admin/inventory-items/:id/location-levels/:location_id
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  location_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "stocked_quantity": "",
    "incoming_quantity": ""
  }
}
