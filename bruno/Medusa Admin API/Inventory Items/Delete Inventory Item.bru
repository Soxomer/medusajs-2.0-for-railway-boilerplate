meta {
  name: Delete Inventory Item
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/admin/inventory-items/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
