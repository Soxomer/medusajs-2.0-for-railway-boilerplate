meta {
  name: Create Inventory Level for Inventory Item
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/admin/inventory-items/:id/location-levels
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "location_id": "",
    "stocked_quantity": "",
    "incoming_quantity": ""
  }
}
