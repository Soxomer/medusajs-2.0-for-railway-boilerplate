meta {
  name: Manage Customers of a Customer Group
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/admin/customer-groups/:id/customers
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "add": [],
    "remove": []
  }
}
