meta {
  name: Delete a Customer Group
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/customer-groups/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
