meta {
  name: Cancel Exchange Request
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/admin/exchanges/:id/request
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
