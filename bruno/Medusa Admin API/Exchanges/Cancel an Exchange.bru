meta {
  name: Cancel an Exchange
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/exchanges/:id/cancel
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "no_notification": ""
  }
}
