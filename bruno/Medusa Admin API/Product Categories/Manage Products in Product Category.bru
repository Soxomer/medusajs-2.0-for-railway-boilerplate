meta {
  name: Manage Products in Product Category
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/admin/product-categories/:id/products
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "add": [],
    "remove": []
  }
}
