meta {
  name: Create Product Tag
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/product-tags
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "value": "",
    "metadata": {}
  }
}
