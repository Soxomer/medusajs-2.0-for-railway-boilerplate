meta {
  name: Delete a Product Tag
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/product-tags/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
