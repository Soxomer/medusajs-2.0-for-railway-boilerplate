meta {
  name: Update a Product Tag
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/product-tags/:id
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "value": "",
    "metadata": {}
  }
}
