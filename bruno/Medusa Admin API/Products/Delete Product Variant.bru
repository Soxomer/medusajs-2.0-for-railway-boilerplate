meta {
  name: Delete Product Variant
  type: http
  seq: 21
}

delete {
  url: {{baseUrl}}/admin/products/:id/variants/:variant_id
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  variant_id: 
}

auth:bearer {
  token: {{token}}
}
