meta {
  name: Update a Product
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/admin/products/:id
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
