meta {
  name: Create a Product Option
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/admin/products/:id/options
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
