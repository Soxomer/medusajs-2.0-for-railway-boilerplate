meta {
  name: Delete an Option from Product
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/admin/products/:id/options/:option_id
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  option_id: 
}

auth:bearer {
  token: {{token}}
}
