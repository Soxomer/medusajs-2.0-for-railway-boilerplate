meta {
  name: Create a Product Variant
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/admin/products/:id/variants
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
