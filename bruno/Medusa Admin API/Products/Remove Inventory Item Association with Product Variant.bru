meta {
  name: Remove Inventory Item Association with Product Variant
  type: http
  seq: 24
}

delete {
  url: {{baseUrl}}/admin/products/:id/variants/:variant_id/inventory-items/:inventory_item_id
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  variant_id: 
  inventory_item_id: 
}

auth:bearer {
  token: {{token}}
}
