meta {
  name: Remove Products from Price List
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/admin/price-lists/:id/products
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "remove": []
  }
}
