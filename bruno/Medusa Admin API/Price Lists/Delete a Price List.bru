meta {
  name: Delete a Price List
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/price-lists/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
