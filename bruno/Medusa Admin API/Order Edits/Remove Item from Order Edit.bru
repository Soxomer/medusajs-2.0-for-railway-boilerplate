meta {
  name: Remove Item from Order Edit
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/admin/order-edits/:id/items/:action_id
  body: none
  auth: bearer
}

params:path {
  id: 
  action_id: 
}

auth:bearer {
  token: {{token}}
}
