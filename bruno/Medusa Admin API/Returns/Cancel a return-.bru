meta {
  name: Cancel a return-
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/admin/returns/:id/cancel
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "no_notification": ""
  }
}
