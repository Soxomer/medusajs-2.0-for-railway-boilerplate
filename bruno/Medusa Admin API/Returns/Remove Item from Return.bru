meta {
  name: Remove Item from Return
  type: http
  seq: 19
}

delete {
  url: {{baseUrl}}/admin/returns/:id/request-items/:action_id
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  action_id: 
}

auth:bearer {
  token: {{token}}
}
