meta {
  name: Update a Received Item in a Return
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/admin/returns/:id/receive-items/:action_id
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  action_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "quantity": "",
    "internal_note": ""
  }
}
