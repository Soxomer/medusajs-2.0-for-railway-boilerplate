meta {
  name: Cancel Return Request
  type: http
  seq: 16
}

delete {
  url: {{baseUrl}}/admin/returns/:id/request
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
