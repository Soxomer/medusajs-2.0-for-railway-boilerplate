meta {
  name: Remove a Received Item from Return
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/admin/returns/:id/receive-items/:action_id
  body: none
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
  action_id: 
}

auth:bearer {
  token: {{token}}
}
