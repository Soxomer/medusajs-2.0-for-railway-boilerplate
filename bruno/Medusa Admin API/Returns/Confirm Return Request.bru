meta {
  name: Confirm Return Request
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/admin/returns/:id/request
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "no_notification": ""
  }
}
