meta {
  name: Generate Reset Password Token for Admin User
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/auth/user/:auth_provider/reset-password
  body: json
  auth: none
}

params:path {
  auth_provider: 
}

body:json {
  {}
}
