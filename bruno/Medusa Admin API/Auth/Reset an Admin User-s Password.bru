meta {
  name: Reset an Admin User-s Password
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/auth/user/:auth_provider/update
  body: json
  auth: none
}

params:query {
  token: 
}

params:path {
  auth_provider: 
}

body:json {
  {}
}
