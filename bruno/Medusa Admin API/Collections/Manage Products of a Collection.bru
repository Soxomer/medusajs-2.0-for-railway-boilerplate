meta {
  name: Manage Products of a Collection
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/admin/collections/:id/products
  body: json
  auth: bearer
}

params:query {
  ~fields: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "add": [],
    "remove": []
  }
}
