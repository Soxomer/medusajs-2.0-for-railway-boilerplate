meta {
  name: Delete a Collection
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/collections/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
